import { ActiveBannerColorPipe } from './active-banner-color.pipe';

describe('ActiveBannerColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveBannerColorPipe();
    expect(pipe).toBeTruthy();
  });
});
