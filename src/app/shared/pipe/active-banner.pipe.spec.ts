import { ActiveBannerPipe } from './active-banner.pipe';

describe('ActiveBannerPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveBannerPipe();
    expect(pipe).toBeTruthy();
  });
});
