import { FieldValuePipe } from './field-value.pipe';

xdescribe('FieldValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FieldValuePipe();
    expect(pipe).toBeTruthy();
  });
});
