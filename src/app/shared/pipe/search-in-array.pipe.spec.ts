import { SearchInArrayPipe } from './search-in-array.pipe';

xdescribe('SearchInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
